# graphify build cache and per-run temp state (regenerated by `graphify update`/`/graphify`)
# Keep the actual deliverables tracked: graph.json, GRAPH_REPORT.md, graph.html, cost.json
graphify-out/cache/
graphify-out/.graphify_*
